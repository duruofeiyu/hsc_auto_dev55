# 依赖（安装产物，不入库）
node_modules/

# 密钥 / 本地配置（含 API key，绝不入库）
.env

# Playwright 运行产物
test-results/
playwright-report/
.cache/
*.log

# midscene 运行产物（每跑一次生成的 HTML 报告 + 日志，可达数 MB）
midscene_run/

# 本地演示页要入库（覆盖仓库根目录 .gitignore 里的 *.html 忽略规则）
!demo/*.html

# macOS
.DS_Store
