# 依赖（安装产物，不入库）
node_modules/

# 密钥 / 本地配置（含 API key，绝不入库）
.env

# Playwright 运行产物
test-results/
playwright-report/
.cache/
*.log

# midscene 运行产物（每跑一次生成的 HTML 报告 + 日志，可达数 MB）
midscene_run/

# macOS
.DS_Store
